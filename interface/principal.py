from interface.fenetre_principale import FenetrePrincipale

fenetre_principale = FenetrePrincipale()
fenetre_principale.mainloop()


# Retourne au début lorsque la partie se termine
# Ajuster les vitesses d'executions des ordinateurs (slider qui controle)
# case attaque encadré (quel case qu'on peut selectionner) et case encadré avec une autre couleur pour defense
